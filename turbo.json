{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "env": ["OPENAI_API_KEY", "NEXT_PUBLIC_SITE_URL", "NEXT_PUBLIC_BASE_PATH"],
      "outputs": [".next/**", "out/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {}
  }
}
